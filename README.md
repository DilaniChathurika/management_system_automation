# management_system_automation
Cypress Automation Framework built using Page Object Model (POM). This project automates key UI components such as radio buttons, checkboxes, dropdowns, alerts, tables, and file uploads. It includes Cypress hooks, fixtures, custom commands, and reusable page objects to create a scalable and maintainable test automation suite.
